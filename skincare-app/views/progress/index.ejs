<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>Skincare Progress Tracking</title>
    <style>
        body {
            background-color: #f8f9fa;
            color: #495057;
        }
        .container {
            max-width: 1200px;
            margin-top: 50px;
        }
        .card {
            border: none;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
            transition: all 0.3s;
        }
        .card:hover {
            transform: translateY(-5px);
        }
        .card-header {
            background-color: #6c757d;
            color: white;
            font-size: 1.2rem;
        }
        .btn-custom {
            background-color: #6c757d;
            border-color: #6c757d;
            color: white;
        }
        .btn-custom:hover {
            background-color: #5a6268;
            border-color: #545b62;
            color: white;
        }
        .card-link {
            color: inherit;
            text-decoration: none;
        }
        .card-link:hover {
            color: inherit;
        }
        .btn-outline-primary {
            color: #007bff;
            border-color: #007bff;
            transition: all 0.3s ease;
        }
        .btn-outline-primary:hover {
            color: #fff;
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-outline-danger {
            color: #dc3545;
            border-color: #dc3545;
            transition: all 0.3s ease;
        }
        .btn-outline-danger:hover {
            color: #fff;
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .navbar {
            background-color: #ffffff;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }
        .navbar-brand {
            font-weight: 700;
            color: #ff69b4;
            font-size: 1.5rem;
        }
        .nav-link {
            color: #4a4a4a;
            font-weight: 500;
            transition: color 0.3s ease;
        }
        .nav-link:hover {
            color: #ff69b4;
        }
    </style>
</head>
<body>
    <!-- Navigation bar -->
    <nav class="navbar navbar-expand-lg navbar-light sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">GlowGuide</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/products">Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/ingredients">Ingredients</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/progress">Progress</a>
                    </li>
                </ul>
                <div class="d-flex">
                    <a href="/login" class="btn btn-dark me-2">SIGN IN</a>
                    <a href="/signup" class="btn btn-outline-dark">SIGN UP</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        <h1 class="text-center mb-5">Skincare Progress Tracking</h1>
        <div class="d-flex justify-content-end mb-4">
            <a href="/addprogress" class="btn btn-success">
                <i class="fas fa-plus"></i> Add New Entry
            </a>
        </div>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <% entries.forEach(entry => { %>
                <div class="col">
                    <a href="/progress/<%= entry.id %>" class="card-link">
                        <div class="card h-100">
                            <div class="card-header">
                                <%= new Date(entry.date).toLocaleDateString() %>
                            </div>
                            <div class="card-body">
                                <p><strong>Skin Condition:</strong> <%= entry.skin_condition %></p>
                                <p><strong>Products Used:</strong> <%= entry.products_used %></p>
                                <p><strong>Notes:</strong> <%= entry.notes %></p>
                            </div>
                            <div class="card-footer bg-transparent border-top-0 d-flex justify-content-end">
                                <a href="/editprogress/<%= entry.id %>" class="btn btn-outline-primary btn-sm me-2">
                                    <i class="fas fa-pencil-alt"></i> Update
                                </a>
                                <a href="/progress/delete/<%= entry.id %>" class="btn btn-outline-danger btn-sm">
                                    <i class="fas fa-trash"></i> Remove
                                </a>
                            </div>
                        </div>
                    </a>
                </div>
            <% }) %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>