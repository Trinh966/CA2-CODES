<!--100 COMPLETED-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>Product Details</title>
    <style>
        body {
            background-color: #f8f9fa;
            color: #495057;
        }
        .container {
            max-width: 800px;
        }
        .product-card {
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
            padding: 2rem;
            margin-top: 2rem;
        }
        .product-image {
            max-width: 100%;
            height: auto;
            border-radius: 10px;
            margin-bottom: 1rem;
        }
        .product-title {
            font-size: 2rem;
            color: #343a40;
            margin-bottom: 1rem;
        }
        .product-info {
            margin-bottom: 0.5rem;
        }
        .info-label {
            font-weight: bold;
            color: #6c757d;
        }
        .btn-back {
            margin-top: 1rem;
        }
        .navbar {
            background-color: #ffffff;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }
        .navbar-brand {
            font-weight: 700;
            color: #ff69b4;
            font-size: 1.5rem;
        }
        .nav-link {
            color: #4a4a4a;
            font-weight: 500;
            transition: color 0.3s ease;
            }

        .nav-link:hover {
            color: #ff69b4;
        }

    </style>
</head>
<body>
    <!-- Navigation bar -->
  <nav class="navbar navbar-expand-lg navbar-light sticky-top">
    <div class="container">
      <a class="navbar-brand" href="/">GlowGuide</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/products">Products</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/ingredients">Ingredients</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/progress">Progress</a>
          </li>
        </ul>
        <div class="d-flex">
          <a href="/login" class="btn btn-dark me-2">SIGN IN</a>
          <a href="/signup" class="btn btn-outline-dark">SIGN UP</a>
        </div>
      </div>
    </div>
  </nav>

    <div class="container">
        <h1 class="text-center my-5">Product Details</h1>
        <% if (product) { %>
            <div class="product-card">
                <img src="<%= product.image %>" class="product-image" alt="<%= product.name %>">
                <h2 class="product-title"><%= product.name %></h2>
                <div class="product-info">
                    <span class="info-label">Brand:</span> <%= product.brand %> ml
                </div>
                <div class="product-info">
                    <span class="info-label">Price:</span> $<%= product.price %>
                </div>
                <div class="product-info">
                    <span class="info-label">Volume:</span> <%= product.volume_ml %> ml
                </div>
                <div class="product-info">
                    <span class="info-label">Type:</span> <%= product.type %>
                </div>
                <div class="product-info">
                    <span class="info-label">Skin Type:</span> <%= product.skin_type %>
                </div>
                <div class="product-info">
                    <span class="info-label">Concerns:</span> <%= product.concerns %>
                </div>
                <div class="product-info">
                    <span class="info-label">Ingredients:</span> <%= product.ingredients %>
                </div>
                <div class="product-info">
                    <span class="info-label">Instructions:</span> <%= product.instructions %>
                </div>


                
            </div>
        <% } else { %>
            <div class="alert alert-warning" role="alert">
                No product found.
            </div>
        <% } %>
        <div class="text-center">
            <a href="/products" class="btn btn-primary btn-lg btn-back"><i class="fas fa-arrow-left"></i> Back to Products</a>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>